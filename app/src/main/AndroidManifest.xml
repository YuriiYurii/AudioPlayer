<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.yuriitsap.audioplayer">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
        <provider android:authorities="com.example.yuriitsap.audioplayer.AudioProvider"
                  android:name=".AudioProvider"
                  android:process=":provider"
                  android:grantUriPermissions="true"
                  android:exported="true"
                  android:enabled="true"
                  android:readPermission="com.example.yuriitsap.READ"
                  android:writePermission="com.example.yuriitsap.WRITE">
        </provider>
        <activity
                android:name=".MainActivity"
                android:label="@string/app_name"
                android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <service android:name=".MusicService" android:exported="true"
                 android:process=":audio"></service>

    </application>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>

</manifest>
